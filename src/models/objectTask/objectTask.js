export class Task {
    constructor(description, priority, timeStamp, status, order){
        this.description = description,
        this.priority = priority,
        this.timeStamp = timeStamp,
        this.status = status,
        this.order = order
    }
}

